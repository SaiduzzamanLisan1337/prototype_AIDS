# Anomaly IDS — Python dependencies
# Install with:  pip install -r requirements.txt

# ── Core data science ─────────────────────────────────────
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# ── Gradient boosting models ──────────────────────────────
xgboost>=2.0
lightgbm>=4.0

# ── Deep Learning (Autoencoder + MLP) ────────────────────
# CPU-only TensorFlow (lighter, works on any laptop):
tensorflow-cpu>=2.13
# If you have an NVIDIA GPU, replace the above line with:
# tensorflow>=2.13

# ── Class imbalance handling ──────────────────────────────
imbalanced-learn>=0.11

# ── Visualisation ─────────────────────────────────────────
matplotlib>=3.7
seaborn>=0.12
